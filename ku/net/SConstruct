import os

env = Environment()
env.Append(HOME = os.environ['HOME'])
KUDIR = env['HOME'] + '/Dropbox/Code/Ku/'

Program('dan', Glob('*.cpp') + [KUDIR + 'gtest/gtest_main.a'], 
    LIBS = ['pthread'],
    CCFLAGS = '-Wall --std=c++0x -g -fPIC -I' + KUDIR
    )
